# Safety

Reserved lane for future shared safety contracts that should live as package-level code or schemas rather than shell tooling.

Current note:
- deterministic shell hooks currently live in `tools/safety-hooks/`
